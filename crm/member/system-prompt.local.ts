// member/system-prompt.local.ts
// Optional system-prompt override. Edited by the member (or by the skill
// /configurar-mi-chatbot). NEVER overwritten on template update.
//
// Return a string to fully replace the generated system prompt, or leave as
// `undefined` to use the default bilingual prompt from src/system-prompt.ts.

export const systemPromptOverride: string | undefined = undefined;
